//打印100~200之间的素数
#include<stdio.h>
int main()
{
int n = 100;
int m = 2;
while(n<=200)
	{
		while (n != m)
		{
			if (n % m > 0)
				m++;
			else break;
		}
			if (n == m)
				printf("%d\n", n);
			n++;
			m = 2;
	}
	return 0;
}
//花了1h
